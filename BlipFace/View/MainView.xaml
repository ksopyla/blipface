<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        
    xmlns:blp="clr-namespace:BlipFace"
        
    xmlns:blpSrv="clr-namespace:BlipFace.Service.Entities;assembly=BlipFace.Service"
    x:Class="BlipFace.View.MainView"
    Title="BlipFace" Height="450" Width="380" 
    
    
    Loaded="Window_Loaded" 
    WindowStyle="ToolWindow">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- ResourceDictionary Source="..\Resource\StatusListDataTemplate.xaml"/ -->
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>


    </Window.Resources>
    <Window.Background>
    	<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
    		<GradientStop Color="#FF3E3D3D" Offset="0"/>
    		<GradientStop Color="#FFFFFFFF" Offset="1"/>
    	</LinearGradientBrush>
    </Window.Background>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition MinHeight="160" Height="*"/>
            <RowDefinition MinHeight="90" MaxHeight="160" Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox  Grid.Column="0" Grid.Row="1"  
			x:Name="lstbStatusList" 
			ScrollViewer.CanContentScroll="True" 
			ScrollViewer.VerticalScrollBarVisibility="Auto"
			ScrollViewer.HorizontalScrollBarVisibility="Disabled"
			FontSize="14"
			HorizontalContentAlignment="Stretch" Margin="0,0,0,0" 
			Background="{x:Null}" BorderBrush="{x:Null}" 
			ItemTemplate="{DynamicResource StatusListData}" BorderThickness="0,0,0,0" ScrollViewer.IsDeferredScrollingEnabled="False" 
			
                  >
           

        </ListBox>

        <Grid Grid.Column="0" Grid.Row="2" Margin="0,0,0,0" RenderTransformOrigin="0.494,0.581" Background="{x:Null}">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>

            </Grid.ColumnDefinitions>

            <TextBox Height="80" 
                     Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                     x:Name="tbMessage" Background="AliceBlue"  BorderThickness="3"
				ClipToBounds="False" TextWrapping="Wrap"  TextChanged="tbMessage_TextChanged" VerticalAlignment="Stretch" FontSize="14">
                <TextBox.BorderBrush>
                    <LinearGradientBrush EndPoint="0,20" StartPoint="0,0" MappingMode="Absolute">
                        <GradientStop Color="#FFABADB3" Offset="0.05"/>
                        <GradientStop Color="#FFE2E3EA" Offset="0.116"/>
                        <GradientStop Color="#FF73AEE9" Offset="1"/>
                    </LinearGradientBrush>
                </TextBox.BorderBrush>
            </TextBox>

            <TextBlock  Grid.Row="1" Grid.Column="0" x:Name="tblCharLeft" FontSize="26" FontWeight="Bold"  Foreground="#a0a0a0" 
				Text="160" HorizontalAlignment="Stretch" Margin="15,0,8,0"/>
            <Button Grid.Column="1" Grid.Row="1" Margin="4,0,2,0" x:Name="btnSendBlip" Content="Blip" Click="btnSendBlip_Click" />
        </Grid>

    </Grid>
</Window>
