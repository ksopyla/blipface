<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:blp="clr-namespace:BlipFace"
        
        xmlns:blpSrv="clr-namespace:BlipFace.Service.Entities;assembly=BlipFace.Service"
        x:Class="BlipFace.View.MainView"
        Title="BlipFace" Height="447" Width="380" 
        BorderThickness="0" FontSize="12" FontWeight="Normal" Loaded="Window_Loaded" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

	<Window.Background>
		<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
			<GradientStop Color="#FF000000" Offset="0"/>
			<GradientStop Color="#FF18124C" Offset="1"/>
		</LinearGradientBrush>
	</Window.Background>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition MinHeight="160" Height="0.664*"/>
			<RowDefinition MinHeight="90" MaxHeight="160" Height="0.336*"/>
		</Grid.RowDefinitions>
        
		<ListBox  Grid.Column="0" Grid.Row="0"  
			x:Name="lstbStatusList" 
			ScrollViewer.CanContentScroll="True" 
			ScrollViewer.VerticalScrollBarVisibility="Visible"
			ScrollViewer.HorizontalScrollBarVisibility="Disabled"
			FontSize="14"
			HorizontalContentAlignment="Stretch" Margin="1,1,1,1" 
			 Background="{x:Null}" BorderBrush="{x:Null}" 
				 >
			<ListBox.ItemTemplate>
				<DataTemplate>
					<Border BorderBrush="#8888dd" Margin="3" Padding="3" 
						BorderThickness="2" CornerRadius="5" Width="Auto" Height="Auto">
						<Grid Background="#FF213B51">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="52"/>
								<ColumnDefinition Width="*" MinWidth="180"/>
							</Grid.ColumnDefinitions>
							<Grid.RowDefinitions>
								<RowDefinition Height="30" />
								<RowDefinition Height="*"  />
								<RowDefinition Height="30" />

							</Grid.RowDefinitions>

							<Label Content="{Binding Path=UserLogin}" Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,8,0" VerticalAlignment="Stretch"/>
							<!--<Label Content="{Binding StatusTime}"  Grid.Column="1" Grid.Row="0"/> -->

							<!--" -->
							<Image   Grid.Column="0" Grid.Row="1" 
								Source="{Binding Path=UserAvatar50}" Height="50" Width="50"
                            RenderTransformOrigin="-0.025,0.52" d:LayoutOverrides="Width" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,4,0,0"
                            />
                               
                            
							<TextBlock TextWrapping="WrapWithOverflow" 
								Text="{Binding Path=Content}" FontWeight="Bold" 
								Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="1" Margin="2,0,8,4.136" Grid.RowSpan="1" Foreground="#FFFFFFFF" />

							<Label Content="{Binding Path=StatusId}" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="1" VerticalAlignment="Stretch" RenderTransformOrigin="0.5,0.5" Margin="0,0,2,0" FontSize="9">
								
							</Label>

							<Label Content="{Binding Path=CreationDate}" Grid.Column="1" Grid.Row="2" RenderTransformOrigin="0.5,0.5" Margin="0,0,0,0" FontSize="9" FontWeight="Bold" Foreground="#FF120D27" HorizontalAlignment="Right">
								
							</Label>
						</Grid>
                        
					</Border>
				</DataTemplate>
			</ListBox.ItemTemplate>

		</ListBox>

		<Grid Grid.Column="0" Grid.Row="1" Margin="8,8,8,8" RenderTransformOrigin="0.494,0.581" Background="{x:Null}">
			<Grid.RowDefinitions>
				<RowDefinition />
				<RowDefinition Height="40"/>
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>

			</Grid.ColumnDefinitions>

			<TextBox MaxHeight="80" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" x:Name="tbMessage" AcceptsReturn="True" Background="AliceBlue"  BorderThickness="3"
				ClipToBounds="False" TextWrapping="Wrap"  TextChanged="tbMessage_TextChanged" VerticalAlignment="Stretch" FontSize="14">
				<TextBox.BorderBrush>
					<LinearGradientBrush EndPoint="0,20" StartPoint="0,0" MappingMode="Absolute">
						<GradientStop Color="#FFABADB3" Offset="0.05"/>
						<GradientStop Color="#FFE2E3EA" Offset="0.116"/>
						<GradientStop Color="#FF73AEE9" Offset="1"/>
					</LinearGradientBrush>
				</TextBox.BorderBrush>
			</TextBox>

			<TextBlock  Grid.Row="1" Grid.Column="0" x:Name="tblCharLeft" FontSize="26" FontWeight="Bold"  Foreground="#a0a0a0" 
				Text="120" HorizontalAlignment="Stretch" Margin="15,0,8,0"/>
			<Button Grid.Column="1" Grid.Row="1" Margin="4,0,2,0" x:Name="btnSendBlip" Content="Blip"/>
		</Grid>

	</Grid>
</Window>
